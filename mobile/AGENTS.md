# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

**Keep this project on Expo SDK 54** so it works with Expo Go from the Play Store.
Do not run `npx expo install` packages that upgrade `expo` to 55+.
